<!--pages/my-post/index.wxml-->
<view>
  <area-header title="我的动态"  showRight="{{false}}"></area-header>
  <view class="post-list">
  <block wx:for="{{myPostList}}" wx:key="postsId"  wx:if="{{myPostList}}">
    <post-item item="{{item}}" bind:typeClick="showPostType" data-postinfo="{{item}}"></post-item>
    <hr/>
  </block>
  <view  wx:if="{{!myPostList}}" class="none-list">该用户似乎很懒，什么帖子都没有</view>
</view>
</view>
